# Calcit 0.14.8 / Calcit 0.14.8

## 中文

- 将项目要求的 Calcit 版本从 0.14.7 升级到 0.14.8，保持 WebSocket C-safe FFI 契约与模块版本不变。
- `--strict-types --check-only` 与静态质量门禁通过。
- 将 CI 中的 GitHub Actions 固定到不可变 commit。

## English

- Upgrade the required Calcit version from 0.14.7 to 0.14.8 while preserving the WebSocket C-safe FFI contract and module version.
- `--strict-types --check-only` and the static quality gate pass.
- Pin GitHub Actions in CI to immutable commits.
