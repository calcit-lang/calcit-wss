# Release 0.2.27 / 发布 0.2.27

- Publish the Calcit 0.13.77 compatibility declaration already verified on
  `main`.
- Preserve the existing WebSocket API, lifecycle behavior, and C-safe native
  ABI; the native crate remains at 0.2.25.

- 发布已在 `main` 验证的 Calcit 0.13.77 兼容性声明。
- 保持现有 WebSocket API、生命周期行为和 C-safe native ABI 不变；
  native crate 版本继续为 0.2.25。
