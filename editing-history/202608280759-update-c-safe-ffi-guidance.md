# Update C-safe FFI guidance / 更新 C-safe FFI 指南

## 中文

- 移除保留 legacy Rust ABI fallback 的过时说明。
- 验证要求改为跟随 `deps.cirru` 声明的 Calcit 版本。

## English

- Remove stale guidance that retained the legacy Rust ABI fallback.
- Make validation follow the Calcit version declared by `deps.cirru`.
